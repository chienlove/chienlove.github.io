name: Sign all IPAs in release with Zsign

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag chứa IPA"
        required: true
      identifier:
        description: "(Tùy chọn) Bundle Identifier mới"
        required: false

jobs:
  sign:
    runs-on: macos-latest
    permissions:
      contents: write
      packages: read

    env:
      GH_TOKEN: ${{ github.token }}
      REPO: ${{ github.repository }}
      SUPABASE_URL: https://zkbzykamrzvvrhodiypk.supabase.co
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Fetch certificate info from Supabase
        run: |
          response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Accept: application/json" \
            "${SUPABASE_URL}/rest/v1/certificates?select=*")
          http_status=$(echo "$response" | grep HTTP_STATUS: | cut -d':' -f2)
          json_data=$(echo "$response" | sed '/HTTP_STATUS/d')
          if [ "$http_status" -ne 200 ]; then
            echo "::error::Supabase API returned $http_status"
            exit 1
          fi
          echo "$json_data" | jq '.[0]' > cert.json
          echo "CERT_P12_URL=$(jq -r '.p12_url' cert.json)" >> $GITHUB_ENV
          echo "CERT_PROVISION_URL=$(jq -r '.provision_url' cert.json)" >> $GITHUB_ENV
          echo "CERT_PASSWORD=$(jq -r '.password' cert.json)" >> $GITHUB_ENV

      - name: Download certificate and provisioning profile
        run: |
          curl -L "$CERT_P12_URL" -o cert.p12
          curl -L "$CERT_PROVISION_URL" -o embedded.mobileprovision

      - name: Extract TEAM_ID from mobileprovision
        run: |
          security cms -D -i embedded.mobileprovision > provision.plist
          TEAM_ID=$(plutil -extract TeamIdentifier.0 xml1 -o - provision.plist | xmllint --xpath 'string(//string)' -)
          echo "TEAM_ID=$TEAM_ID" >> $GITHUB_ENV

      - name: Set workflow inputs
        run: |
          echo "TAG=${{ github.event.inputs.tag }}" >> $GITHUB_ENV
          echo "IDENTIFIER=${{ github.event.inputs.identifier }}" >> $GITHUB_ENV

      - name: Verify release exists
        run: gh release view "$TAG" --repo "$REPO"

      - name: Download all IPA files from release
        run: |
          gh release download "$TAG" --repo "$REPO" --pattern "*.ipa" --clobber
          COUNT=$(ls *.ipa 2>/dev/null | wc -l)
          if [ "$COUNT" -eq 0 ]; then
            echo "::error::❌ No IPA files found in release"
            exit 1
          fi

      - name: Install Zsign
        run: |
          brew install pkg-config openssl minizip
          git clone https://github.com/zhlynn/zsign.git
          cd zsign/build/macos && make clean && make
          sudo cp ../../bin/zsign /usr/local/bin/

      - name: Patch Info.plist & Binary if identifier is set
        if: ${{ github.event.inputs.identifier != '' }}
        run: |
          for IPA in *.ipa; do
            echo "📦 Patching $IPA..."
            APP_NAME=$(basename "$IPA" .ipa)
            WORKDIR="/tmp/zsign_patch_$RANDOM"
            unzip -q "$IPA" -d "$WORKDIR"

            APP_DIR=$(find "$WORKDIR/Payload" -name "*.app" -type d | head -n1)
            PLIST_PATH="$APP_DIR/Info.plist"
            BINARY_NAME=$(/usr/libexec/PlistBuddy -c "Print :CFBundleExecutable" "$PLIST_PATH")
            BINARY_PATH="$APP_DIR/$BINARY_NAME"

            OLD_ID=$(/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$PLIST_PATH")
            echo "🔍 OLD Identifier: $OLD_ID"
            echo "✏️  Setting CFBundleIdentifier to: $IDENTIFIER"

            /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier $IDENTIFIER" "$PLIST_PATH"
            /usr/bin/perl -pi -e "s/\Q$OLD_ID\E/$IDENTIFIER/" "$BINARY_PATH"

            NEW_ID=$(/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$PLIST_PATH")
            echo "✅ Patched Info.plist: $NEW_ID"

            if [[ "$NEW_ID" != "$IDENTIFIER" ]]; then
              echo "::error::❌ Failed to patch CFBundleIdentifier"
              exit 1
            fi

            cd "$WORKDIR"
            zip -qr "../../$APP_NAME-patched.ipa" Payload
            cd -
            mv "$APP_NAME-patched.ipa" "$IPA"
            rm -rf "$WORKDIR"
          done

      - name: Sign all IPA files with Zsign
        run: |
          mkdir -p signed
          for IPA in *.ipa; do
            APP_NAME=$(basename "$IPA" .ipa)
            OUT_NAME="signed/$APP_NAME-signed.ipa"

            if [ -z "$IDENTIFIER" ]; then
              echo "🔧 Signing $IPA without changing Bundle ID..."
              zsign -f -k cert.p12 -p "$CERT_PASSWORD" -m embedded.mobileprovision -o "$OUT_NAME" "$IPA"
            else
              echo "🔧 Signing $IPA with new Bundle ID: $IDENTIFIER..."
              echo '<?xml version="1.0" encoding="UTF-8"?>' > entitlements.plist
              echo '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' >> entitlements.plist
              echo '<plist version="1.0"><dict>' >> entitlements.plist
              echo '  <key>application-identifier</key>' >> entitlements.plist
              echo "  <string>$TEAM_ID.$IDENTIFIER</string>" >> entitlements.plist
              echo '</dict></plist>' >> entitlements.plist

              zsign -f -k cert.p12 -p "$CERT_PASSWORD" -m embedded.mobileprovision \
                -b "$IDENTIFIER" -e entitlements.plist -o "$OUT_NAME" "$IPA"
            fi
          done

      - name: Verify embedded.mobileprovision exists in signed IPA
        run: |
          for FILE in signed/*.ipa; do
            unzip -q "$FILE" -d temp
            PROFILE=$(find temp/Payload -name "embedded.mobileprovision" | head -n1)
            if [ -z "$PROFILE" ]; then
              echo "::error::❌ Missing embedded.mobileprovision in $FILE"
              exit 1
            fi
            rm -rf temp
          done

      - name: Upload signed IPA and plist
        run: |
          BASE_URL="https://github.com/$REPO/releases/download/$TAG"
          for FILE in signed/*.ipa; do
            FILENAME=$(basename "$FILE")
            APP_NAME="${FILENAME%.ipa}"
            PLIST_NAME="${APP_NAME}.plist"

            gh release upload "$TAG" "$FILE" --repo "$REPO" --clobber

            echo "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" > "$PLIST_NAME"
            echo "<!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">" >> "$PLIST_NAME"
            echo "<plist version=\"1.0\"><dict>" >> "$PLIST_NAME"
            echo "  <key>items</key><array><dict>" >> "$PLIST_NAME"
            echo "    <key>assets</key><array><dict>" >> "$PLIST_NAME"
            echo "      <key>kind</key><string>software-package</string>" >> "$PLIST_NAME"
            echo "      <key>url</key><string>$BASE_URL/$FILENAME</string>" >> "$PLIST_NAME"
            echo "    </dict></array>" >> "$PLIST_NAME"
            echo "    <key>metadata</key><dict>" >> "$PLIST_NAME"
            echo "      <key>bundle-identifier</key><string>${IDENTIFIER:-default}</string>" >> "$PLIST_NAME"
            echo "      <key>bundle-version</key><string>1.0.0</string>" >> "$PLIST_NAME"
            echo "      <key>kind</key><string>software</string>" >> "$PLIST_NAME"
            echo "      <key>title</key><string>$APP_NAME</string>" >> "$PLIST_NAME"
            echo "    </dict>" >> "$PLIST_NAME"
            echo "  </dict></array></dict></plist>" >> "$PLIST_NAME"

            gh release upload "$TAG" "$PLIST_NAME" --repo "$REPO" --clobber
            echo "📲 Install link:"
            echo "itms-services://?action=download-manifest&url=$BASE_URL/$PLIST_NAME"
          done