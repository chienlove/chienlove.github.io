{
  "name": "ipa-downloader",
  "version": "1.0.0",
  "description": "IPA Downloader Cloudflare Worker",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/index.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "wrangler": "^3.0.0"
  }
}